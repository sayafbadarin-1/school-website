<div class="text-center py-5">
    <h1 class="fw-black mb-3" style="font-weight: 800; font-size: 2.5rem;">مرحباً بك في <span class="text-primary">مكتبة الحادي عشر</span></h1>
    <p class="text-muted fs-5 mb-5">اختر الفرع للبدء في تصفح المواد والملخصات</p>
    
    <% if(branches.length > 0) { %>
        <div class="row justify-content-center g-4">
            <% branches.forEach((branch, index) => { %>
                <% 
                   const colors = ['primary', 'success', 'danger', 'warning', 'info'];
                   const color = colors[index % colors.length];
                %>
                <div class="col-md-4 col-sm-6">
                    <a href="/branch/<%= branch._id %>" class="text-decoration-none">
                        <div class="card h-100 p-4 text-center border-0 shadow-sm position-relative" style="transition: all 0.3s;">
                            <div class="card-body">
                                <div class="icon-box mb-3 mx-auto rounded-circle d-flex align-items-center justify-content-center" 
                                     style="width: 80px; height: 80px; background-color: var(--bg-light);">
                                    <i class="fas fa-book-open fs-2 text-<%= color %>"></i>
                                </div>
                                <h3 class="fw-bold text-dark"><%= branch.name %></h3>
                                <p class="text-muted">اضغط للدخول وتصفح المواد</p>
                            </div>
                            <div class="card-footer bg-transparent border-0">
                                <span class="btn btn-outline-<%= color %> w-100 rounded-pill">تصفح الآن</span>
                            </div>
                        </div>
                    </a>
                </div>
            <% }) %>
        </div>
    <% } else { %>
        <div class="alert alert-info py-4 shadow-sm border-0 rounded-4">
            <i class="fas fa-info-circle me-2"></i> لا يوجد فروع مضافة حالياً.
        </div>
    <% } %>
</div>
