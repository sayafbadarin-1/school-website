<div class="d-flex justify-content-between align-items-center mt-4 mb-4">
    <h2>👋 أهلاً بك يا، <span class="text-primary"><%= username %></span></h2>
    <div>
        <button class="btn btn-outline-primary ms-2" data-bs-toggle="modal" data-bs-target="#changePassModal">
            <i class="fas fa-key"></i> تغيير كلمة مروري
        </button>
        <a href="/logout" class="btn btn-danger">تسجيل خروج</a>
    </div>
</div>

<div class="modal fade" id="changePassModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">تغيير كلمة المرور</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <form action="/admin/change-password" method="POST">
                <div class="modal-body">
                    <label>كلمة المرور الجديدة</label>
                    <input type="password" name="newPassword" class="form-control" required>
                </div>
                <div class="modal-footer">
                    <button type="submit" class="btn btn-primary">حفظ</button>
                </div>
            </form>
        </div>
    </div>
</div>

<div class="card shadow mb-4 border-warning">
    <div class="card-header bg-warning text-dark fw-bold">
        <i class="fas fa-bullhorn"></i> شريط الإعلانات العاجلة
    </div>
    <div class="card-body">
        <form action="/admin/update-ticker" method="POST">
            <div class="mb-3">
                <label class="form-label">نص الإعلان:</label>
                <input type="text" name="content" class="form-control" value="<%= ticker ? ticker.content : '' %>" placeholder="اكتب الإعلان هنا..." required>
            </div>
            <div class="form-check form-switch mb-3">
                <input class="form-check-input" type="checkbox" name="isActive" id="tickerSwitch" <%= (ticker && ticker.isActive) ? 'checked' : '' %>>
                <label class="form-check-label" for="tickerSwitch">تفعيل الشريط في الموقع</label>
            </div>
            <button type="submit" class="btn btn-dark btn-sm">حفظ التغييرات</button>
        </form>
    </div>
</div>

<% if (isSuperAdmin) { %>
    <div class="card shadow mb-4">
        <div class="card-header bg-dark text-white">
            <i class="fas fa-users-cog"></i> إدارة المشرفين
        </div>
        <div class="card-body">
            <form action="/admin/add-admin" method="POST" class="row g-2 align-items-center mb-3">
                <div class="col-auto"><input type="text" name="username" class="form-control" placeholder="اسم المستخدم" required></div>
                <div class="col-auto"><input type="password" name="password" class="form-control" placeholder="كلمة المرور" required></div>
                <div class="col-auto"><button type="submit" class="btn btn-success">إضافة مشرف</button></div>
            </form>
            <ul class="list-group">
                <% admins.forEach(admin => { %>
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        <span><%= admin.username %> (<%= admin.role %>)</span>
                        <% if(admin.role !== 'superadmin') { %>
                            <a href="/admin/delete-admin/<%= admin._id %>" class="btn btn-sm btn-danger">حذف</a>
                        <% } %>
                    </li>
                <% }) %>
            </ul>
        </div>
    </div>
<% } %>

<div class="row">
    <div class="col-12">
        <div class="card shadow">
            <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
                <h5 class="mb-0">الفروع الدراسية</h5>
                <button class="btn btn-light btn-sm" data-bs-toggle="modal" data-bs-target="#addBranchModal">
                    <i class="fas fa-plus"></i> إضافة فرع
                </button>
            </div>
            <div class="card-body">
                <% if(branches.length > 0) { %>
                    <ul class="list-group">
                        <% branches.forEach(branch => { %>
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <h5 class="mb-0"><%= branch.name %></h5>
                                <div>
                                    <button class="btn btn-sm btn-warning ms-1" data-bs-toggle="modal" data-bs-target="#editModal<%= branch._id %>">
                                        <i class="fas fa-edit"></i>
                                    </button>
                                </div>
                            </li>
                        <% }) %>
                    </ul>
                <% } else { %>
                    <div class="p-4 text-center text-muted">لا يوجد فروع مضافة بعد.</div>
                <% } %>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="addBranchModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">إضافة فرع جديد</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <form action="/admin/add-branch" method="POST">
                <div class="modal-body">
                    <input type="text" name="name" class="form-control" placeholder="اسم الفرع (علمي، أدبي..)" required>
                </div>
                <div class="modal-footer">
                    <button type="submit" class="btn btn-success">إضافة</button>
                </div>
            </form>
        </div>
    </div>
</div>

<% if(branches.length > 0) { %>
    <% branches.forEach(branch => { %>
        <div class="modal fade" id="editModal<%= branch._id %>" tabindex="-1" aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title">تعديل اسم الفرع</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                    </div>
                    <form action="/admin/edit-branch/<%= branch._id %>" method="POST">
                        <div class="modal-body">
                            <input type="text" name="name" class="form-control" value="<%= branch.name %>" required>
                        </div>
                        <div class="modal-footer">
                            <button type="submit" class="btn btn-primary">حفظ</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    <% }) %>
<% } %>
