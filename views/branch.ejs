<div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="fw-bold text-primary"><i class="fas fa-folder-open"></i> قسم: <%= branch.name %></h2>
    <a href="/" class="btn btn-secondary"><i class="fas fa-arrow-right"></i> رجوع</a>
</div>

<% if (isAdmin) { %>
    <button type="button" class="btn btn-success mb-4 shadow" data-bs-toggle="modal" data-bs-target="#addBookModal">
        <i class="fas fa-plus"></i> إضافة كتاب جديد
    </button>
<% } %>

<div class="row">
    <% if(books.length > 0) { %>
        <% books.forEach(book => { %>
            <div class="col-6 col-md-3 mb-4">
                <div class="card h-100 book-card text-center p-3 shadow-sm border-0">
                    <a href="/book/<%= book._id %>" class="text-decoration-none">
                        <img src="<%= book.image %>" class="card-img-top rounded mx-auto d-block" style="height: 150px; object-fit: contain;" alt="<%= book.name %>" onerror="this.src='https://via.placeholder.com/150?text=No+Image'">
                        <div class="card-body pb-0">
                            <h5 class="card-title fw-bold mt-2 text-dark"><%= book.name %></h5>
                        </div>
                    </a>
                    
                    <% if (isAdmin) { %>
                        <div class="card-footer bg-transparent border-0 mt-2">
                            <div class="btn-group w-100" role="group">
                                <button class="btn btn-outline-warning btn-sm" data-bs-toggle="modal" data-bs-target="#editBookModal<%= book._id %>">
                                    <i class="fas fa-pen"></i> تعديل
                                </button>
                                <a href="/admin/delete-book/<%= book._id %>" class="btn btn-outline-danger btn-sm" onclick="return confirm('حذف؟')">
                                    <i class="fas fa-trash"></i> حذف
                                </a>
                            </div>
                        </div>
                    <% } %>
                </div>
            </div>

            <% if (isAdmin) { %>
                <div class="modal fade" id="editBookModal<%= book._id %>" tabindex="-1">
                    <div class="modal-dialog">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title">تعديل الكتاب</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                            </div>
                            <form action="/admin/edit-book/<%= book._id %>" method="POST">
                                <div class="modal-body">
                                    <div class="mb-3">
                                        <label>اسم الكتاب</label>
                                        <input type="text" name="name" class="form-control" value="<%= book.name %>" required>
                                    </div>
                                    <div class="mb-3">
                                        <label>رابط الصورة</label>
                                        <input type="url" name="coverImage" class="form-control" value="<%= book.image %>" required>
                                    </div>
                                </div>
                                <div class="modal-footer">
                                    <button type="submit" class="btn btn-primary">حفظ</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            <% } %>
        <% }) %>
    <% } else { %>
        <div class="col-12 text-center text-muted py-5">
            <h4>لا يوجد كتب مضافة هنا بعد 📭</h4>
        </div>
    <% } %>
</div>

<div class="modal fade" id="addBookModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-success text-white">
                <h5 class="modal-title">إضافة كتاب</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <form action="/admin/add-book" method="POST">
                <div class="modal-body">
                    <input type="hidden" name="branchId" value="<%= branch._id %>">
                    <div class="mb-3">
                        <label>اسم الكتاب</label>
                        <input type="text" name="name" class="form-control" required>
                    </div>
                    <div class="mb-3">
                        <label>رابط الصورة</label>
                        <input type="url" name="coverImage" class="form-control" placeholder="https://..." required>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="submit" class="btn btn-success">إضافة</button>
                </div>
            </form>
        </div>
    </div>
</div>
