<div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="fw-bold text-primary">
        <i class="fas fa-folder-open"></i> قسم: <%= branch.name %>
    </h2>
    <a href="/" class="btn btn-secondary">
        <i class="fas fa-arrow-right"></i> رجوع
    </a>
</div>

<% if (isAdmin) { %>
    <button type="button" class="btn btn-success mb-4 shadow" data-bs-toggle="modal" data-bs-target="#addBookModal">
        <i class="fas fa-plus"></i> إضافة كتاب جديد
    </button>
<% } %>

<div class="row">
    <% if(books.length > 0) { %>
        <% books.forEach(book => { %>
            <div class="col-6 col-md-3 mb-4 position-relative">
                
                <% if (isAdmin) { %>
                    <a href="/admin/delete-book/<%= book._id %>" class="btn btn-danger btn-sm position-absolute top-0 end-0 m-2" style="z-index: 10;" onclick="return confirm('هل أنت متأكد من حذف هذا الكتاب؟')">
                        <i class="fas fa-times"></i>
                    </a>
                <% } %>

                <a href="/book/<%= book._id %>" class="text-decoration-none">
                    <div class="card h-100 book-card text-center p-3">
                        <img src="<%= book.image %>" class="card-img-top rounded mx-auto d-block" style="height: 150px; object-fit: contain;" alt="<%= book.name %>" onerror="this.src='https://via.placeholder.com/150?text=No+Image'">
                        <div class="card-body">
                            <h5 class="card-title fw-bold mt-2 text-dark"><%= book.name %></h5>
                        </div>
                    </div>
                </a>
            </div>
        <% }) %>
    <% } else { %>
        <div class="col-12 text-center text-muted py-5">
            <h4>لا يوجد كتب مضافة في هذا الفرع بعد 📭</h4>
        </div>
    <% } %>
</div>

<div class="modal fade" id="addBookModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header bg-success text-white">
                <h5 class="modal-title">إضافة كتاب جديد</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <form action="/admin/add-book" method="POST">
                <div class="modal-body">
                    <input type="hidden" name="branchId" value="<%= branch._id %>">
                    
                    <div class="mb-3">
                        <label class="form-label">اسم الكتاب</label>
                        <input type="text" name="name" class="form-control" required>
                    </div>
                    
                    <div class="mb-3">
                        <label class="form-label">رابط صورة الغلاف (URL)</label>
                        <input type="url" name="coverImage" class="form-control" placeholder="https://example.com/image.jpg" required>
                        <small class="text-muted">انسخ رابط الصورة من جوجل والصقه هنا</small>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">إلغاء</button>
                    <button type="submit" class="btn btn-success">حفظ الكتاب</button>
                </div>
            </form>
        </div>
    </div>
</div>